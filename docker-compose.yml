version: "3.9"

services:

  ollama:

    image: ollama/ollama

    container_name: ollama

    ports:
      - "11434:11434"

    volumes:
      - ollama:/root/.ollama

    restart: unless-stopped

  floramind:

    build: .

    container_name: floramind

    ports:
      - "8000:8000"

    volumes:
      - ./data:/app/data
      - ./chroma_db:/app/chroma_db

    depends_on:
      - ollama

    restart: unless-stopped

volumes:

  ollama: