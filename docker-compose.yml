services:
  aloe-api:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - ./plants.json:/app/plants.json:ro
      - ./chroma_db:/app/chroma_db:ro
    env_file:
      - .env
    restart: unless-stopped